version: '3'

services:
  dbGitlab:
    image: postgres:latest
    restart: always
    container_name: dbGitlab
    environment:
      POSTGRES_PASSWORD: '${git_db_pass}'
      POSTGRES_USER: '${git_db_username}'
      POSTGRES_DB: '${git_db_name}'
    volumes:
      - ./postgresGitlab:/var/lib/postgresql/data
    networks:
      - gitlabnet

  gitlab:
    container_name: gitlab
    image: gitlab/gitlab-ce:latest
    restart: always
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://gitlab';
        gitlab_rails['db_adapter'] = 'postgresql';
        gitlab_rails['db_encoding'] = 'unicode';
        gitlab_rails['db_host'] = 'dbGitlab';
        gitlab_rails['db_port'] = '5432';
        gitlab_rails['db_username'] = '${git_db_username}'
        gitlab_rails['db_password'] = ${git_db_pass}'
        gitlab_rails['db_database'] = '${git_db_name}'
        postgresql['enable'] = false
    ports:
      - "80:80"
    volumes:
      - ./Gitlab/app:/etc/gitlab
      - ./Gitlab/logs:/var/log/gitlab
      - ./Gitlab/data:/var/opt/gitlab
    depends_on:
      - dbGitlab
    networks:
      - gitlabnet

  gitlab-runner:
    image: gitlab/gitlab-runner:latest
    container_name: gitlab-runner
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./runner:/etc/gitlab-runner
    depends_on:
      - gitlab
    networks:
      - gitlabnet

  sonarqube:
    image: sonarqube:lts-community
    depends_on:
      - sonar_db
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://sonar_db:5432/sonar
      SONAR_JDBC_USERNAME: sonar
      SONAR_JDBC_PASSWORD: sonar
    ports:
      - "9001:9000"
    volumes:
      - ./sonar/sonarqube_conf:/opt/sonarqube/conf
      - ./sonar/sonarqube_data:/opt/sonarqube/data
      - ./sonar/sonarqube_extensions:/opt/sonarqube/extensions
      - ./sonar/sonarqube_logs:/opt/sonarqube/logs
      - ./sonar/sonarqube_temp:/opt/sonarqube/temp
    networks:
      - gitlabnet

  sonar_db:
    image: postgres:13
    environment:
      POSTGRES_USER: sonar
      POSTGRES_PASSWORD: sonar
      POSTGRES_DB: sonar
    volumes:
      - ./sonar/db/sonar_db:/var/lib/postgresql
      - ./sonar/dbdata/sonar_db_data:/var/lib/postgresql/data
    networks:
      - gitlabnet

  grafana:
    image: grafana/grafana-enterprise
    container_name: grafana
    restart: unless-stopped
    ports:
      - '3000:3000'
      
    volumes:
      - ./grafana-storage:/var/lib/grafana
      - custom.ini:/etc/grafana/custom.ini

    environment:
    - GF_PATHS_CONFIG=/etc/grafana/custom.ini

networks:
  gitlabnet:    
    driver: bridge

